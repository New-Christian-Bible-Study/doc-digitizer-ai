# Optional: PaddleOCR line boxes (transcribe-chunk + convert-transcription-boxes).
# Install after base: pip install -r requirements.txt -r requirements-paddleocr.txt
setuptools>=69.0.0
paddlepaddle>=2.6.0,<3.0.0
paddleocr>=2.7.0,<3.0.0
